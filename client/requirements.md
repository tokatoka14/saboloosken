## Packages
framer-motion | Page transitions and scroll-triggered animations for the multi-step wizard
react-dropzone | Beautiful and robust file upload handling for the ID scanning and document steps
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to intuitively merge Tailwind CSS classes without style conflicts

## Notes
- Assumes `@shared/routes` and `@shared/schema` exist and export `api`, `submissionSchema`, `SubmissionInput`
- Using `Plus Jakarta Sans` for display and `Inter` for body typography.
- Auth requires `credentials: "include"` for session management.
